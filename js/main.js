document.addEventListener('DOMContentLoaded', function() {
    try {
        // Kode initialization di sini
    } catch (error) {
        console.error('Initialization error:', error);
        alert('Terjadi kesalahan saat memuat aplikasi. Silakan refresh halaman.');
    }
});
